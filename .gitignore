node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Data: downloaded sources are large / regenerable. Keep the versioned eval set,
# and the prefabbricated DB (committed via Git LFS) needed by the HF Spaces deploy.
/data/*
!/data/eval/
!/data/judge.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
