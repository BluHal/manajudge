# OS
.DS_Store
Thumbs.db

# Node (safety net for any root-level tooling; backend has its own)
node_modules/

# Env (safety net; each context keeps its own .env)
.env
.env.*
!.env.example

# Per-context ignores live in backend/.gitignore and app/.gitignore
